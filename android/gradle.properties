org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Disable Kotlin incremental compilation. The project (D:) and the pub cache
# holding plugin Kotlin sources like webview_flutter_android (C:) live on
# different drives; Kotlin's incremental compiler can't compute a relative path
# across drive roots and crashes the daemon. Full compilation avoids that path.
kotlin.incremental=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
